<div id="pending-payment-container" class="payment-method-container d-none">
    <div class="card shadow-sm mb-4 border-warning">
        <div class="card-header bg-warning text-dark d-flex align-items-center">
            <i class="bi bi-hourglass-split me-2 fs-4"></i>
            <h5 class="mb-0">Pending Payment</h5>
        </div>
        <div class="card-body p-4">
            <div class="text-center mb-4">
                <i class="bi bi-hourglass-split text-warning mb-3" style="font-size: 3rem;"></i>
                <h6 class="mb-3">Create bill now, collect payment later</h6>
            </div>
            
            <div class="alert alert-warning mb-4 shadow-sm">
                <div class="d-flex">
                    <div class="me-3">
                        <i class="bi bi-info-circle-fill"></i>
                    </div>
                    <div>
                        <p class="mb-0">The bill will be created with a pending payment status for <span class="fw-bold">₹<span id="pendingAmountDisplay">0.00</span></span>.</p>
                        <p class="mb-0 mt-2 small">You can update the payment status later when payment is received.</p>
                    </div>
                </div>
            </div>
            
            <div class="payment-details p-3 bg-light rounded mb-4">
                <div class="d-flex justify-content-between mb-2">
                    <span class="text-muted">Amount Due:</span>
                    <span class="fw-bold text-warning">₹<span id="pendingAmountTotal">0.00</span></span>
                </div>
                <div class="d-flex justify-content-between">
                    <span class="text-muted">Status:</span>
                    <span class="badge bg-warning text-dark">Pending</span>
                </div>
            </div>
            
            <div class="d-grid gap-2">
                <button type="submit" class="btn btn-warning btn-lg px-4 rounded-pill shadow-sm text-dark" id="pendingSubmitBtn">
                    <i class="bi bi-file-earmark-check me-2"></i>Create Bill
                </button>
            </div>
        </div>
        <div class="card-footer bg-light border-top-0 text-center">
            <small class="text-muted">
                <i class="bi bi-clock-history me-1"></i>Payment can be collected at a later time
            </small>
        </div>
    </div>
</div>